

## Remove Icon from Account Onboarding Progress Widget

### Change (single file)

**`src/components/dashboard/OnboardingProgressWidget.tsx`**
- Remove the `ClipboardList` icon element from the CardHeader (line 38)
- Remove the `ClipboardList` import from lucide-react (line 4)

The `GripVertical` drag handle icon remains unchanged.

