

## Reduce Email Table Header Height

### Change
Reduce padding in the table header row and body rows from `p-3` to `py-1.5 px-3` (halving vertical padding) across the `<thead>` and `<tbody>` cells in `src/pages/Email.tsx` (lines 386–449).

### File
| File | Change |
|------|--------|
| `src/pages/Email.tsx` | Change all `p-3` in table `<th>` and `<td>` elements to `py-1.5 px-3` to reduce row height |

